
<button
  id="button-{{this.id}}"
  type="button"
  class="phrase-button btn btn-secondary border histBtn m-2 col-6 rounded"
  data-upperText="{{this.upper_text}}"
  data-lowerText="{{this.lower_text}}"
><h6 class="text-dark">UPPER TEXT</h6><p class=" uppercase text-light">"{{this.upper_text}}"</p>
<hr>
  <h6 class="text-dark">LOWER TEXT</h6><p class="uppercase text-light">"{{this.lower_text}}"</p>
</button>